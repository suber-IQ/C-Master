// 👉 Get ASCII Value of Character

#include<iostream>
using namespace std;

int main(){
      
      // ❓ Get ASCII Value(integer/number) of Char
//       char ch;
//      unsigned int x;
//       cout << "Enter a character: " << endl;
//       cin>>ch;
//       x=ch;
//       cout << "character: " << ch << " is ASCII Value: "<< x << endl;

      // ❓ Get ASCII Char of Integer(value/number) (🌟 Opposite)
      char ch;
     unsigned int x;
      cout << "Enter a Number to get ASCII Character: " << endl;
      cin>>x;
      ch=x;
      cout << "Number of ASCII: " << x << " of character: " << ch << endl;

      return 0;
}